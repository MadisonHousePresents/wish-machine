<%= form_for(wish) do |f| %>
  <% if wish.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(wish.errors.count, "error") %> prohibited this wish from being saved:</h2>

      <ul>
      <% wish.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :participant_requirement %>
    <%= f.text_field :participant_requirement %>
  </div>

  <div class="field">
    <%= f.label :ef_fulfillment %>
    <%= f.text_field :ef_fulfillment %>
  </div>

  <div class="field">
    <%= f.label :approved %>
    <%= f.check_box :approved %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
